# EncodeForge Runtime Requirements
# Python 3.10+ required
#
# Only packages actually imported by the application belong here. Build and
# development tooling lives in requirements-dev.txt, so `pip install .` does not
# drag a compiler toolchain onto end-user machines.

# ==================== UI Framework ====================
PySide6>=6.6.0
PySide6-Addons>=6.6.0
PySide6-Essentials>=6.6.0
qtawesome>=1.3.0

# ==================== Notifications ====================
desktop-notifier>=3.5.0

# ==================== HTTP & Web Scraping ====================
requests>=2.31.0
beautifulsoup4>=4.12.0

# ==================== AI/ML - Subtitle Generation ====================
# faster-whisper: CTranslate2 backend — much faster than openai-whisper,
# no manual PyTorch/CUDA wheel selection required.
# The app can also install this on demand via Tools > Setup AI Subtitles.
faster-whisper>=1.2.1

# ==================== CLI ====================
click>=8.1.7

# ==================== Utilities ====================
packaging>=23.0

# ==================== Deliberately removed ====================
# The packages below were declared here but are not imported anywhere in
# app/, core/, utils/, main.py or cli.py. Two were actively harmful:
#
#   pathlib             - abandoned Python 2 backport that SHADOWS the stdlib
#                         module on Python 3 and is a known Nuitka breaker
#   PyQt-Fluent-Widgets - unused, and pulls PyQt5 into a PySide6 application,
#                         putting two Qt bindings in one process
#
# Merely unused: pandas, numpy (~100 MB), lxml (every BeautifulSoup call uses
# 'html.parser'), chardet, python-magic-bin, python-dotenv, pyyaml, colorlog,
# tqdm, tmdbv3api, curl-cffi, httpx, ffmpeg-python, mutagen,
# concurrent-log-handler.
#
# Build tooling (Nuitka, ordered-set, zstandard) moved to requirements-dev.txt.
# Re-add any of these in the same commit as the code that imports them.
